import { Stack } from "@mui/material";

const Login = () => {
  return (
    <Stack
      sx={{
        flexGrow: 1,
        alignItems: "center",
      }}
    >
      <h1>Login</h1>
    </Stack>
  );
};

export default Login;
